{% for jorb in range(1, batch_size + 1) %}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: '{{ jorb|int + current_jorb|int }}'
  namespace: default
spec:
  template:
    metadata:
      name: '{{ jorb|int + current_jorb|int }}'
      namespace: default
      labels:
        type: '50k'
    spec:
      containers:
      - name: test
        image: alpine:3
        command: ['echo', 'complete']
      restartPolicy: Never
{% endfor %}
